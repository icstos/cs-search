# Roadmap
参考Everything实现本地文件、内容搜索功能
```ini
# 项目目标
使用 Python(3.12+) + Flet（0.86.5） 框架 + everytools库，在 Windows 平台复刻 Everything 文件搜索工具。采用声明式 UI 架构，实现原生级极速搜索体验，界面简洁高效，操作顺滑无卡顿。

## 技术栈强制约束
1.  UI 框架：Flet（严格遵循声明式写法，基于控件组合构建界面）
2.  搜索内核：everytools（适配 Everything 1.5+，兼容文件名搜索与内容搜索）
3.  异步规则：所有 SDK 同步调用必须通过 `asyncio.to_thread` 放入线程池执行，**严禁阻塞 Flet 事件循环**，保证 UI 全程无冻结
4.  配套依赖：
    - 文件操作：使用系统原生 API（os.startfile、subprocess 调用资源管理器并选中文件）
    - 剪贴板：pyperclip
    - 全局热键：pynput
    - 回收站删除：send2trash
    - 数据持久化：本地 JSON 文件存储书签与配置
5.  运行平台：Windows 10/11，适配高 DPI 显示

## 核心功能（必须完整实现）
### 1. 极速搜索引擎
- 实时输入搜索：搜索框输入防抖 300ms，输入即触发搜索，结果毫秒级回显
- 搜索语法：完整支持 Everything 原生语法，包括关键词、通配符、正则表达式、`ext:` 后缀筛选、`content:` 内容搜索
- 结果排序：支持按文件名、路径、大小、修改时间排序，表头点击切换升降序
- 高性能列表：结果懒加载，首次默认加载前 200 条，滚动触底增量加载，避免万级结果导致 UI 卡顿
- 状态反馈：底部状态栏实时显示命中结果总数、搜索耗时、Everything 索引状态

### 2. 实时索引同步
- 监听 Everything 索引变更事件，当系统发生文件新建、重命名、删除、移动操作时，自动静默刷新当前搜索结果
- 界面无闪烁增量更新，不打断用户当前选中状态
- 程序启动时自动检测 Everything 服务运行状态，未启动则给出友好提示与启动引导

### 3. 全量文件操作（右键菜单 + 快捷键）
右键菜单包含以下操作，全部支持系统原生交互，支持多选批量执行：
- 打开文件：调用系统默认程序打开选中文件
- 打开文件所在位置：在资源管理器中打开并高亮选中对应文件
- 复制完整路径：将文件绝对路径写入剪贴板
- 复制文件名：仅将文件名写入剪贴板
- 删除文件：支持删除到回收站与永久删除两种模式，删除前二次确认弹窗

### 4. 多维过滤器
- 左侧常驻侧边栏，提供分类快捷过滤器，全部与当前搜索关键词叠加生效
- 内置分类：全部、文件夹、文档、图片、视频、音频、压缩包、可执行文件
- 支持按修改时间范围筛选（今天、本周、本月、本年）
- 支持按文件大小区间筛选

### 5. 书签系统
- 支持将当前搜索条件（关键词+过滤器组合）保存为书签，可自定义命名
- 书签持久化存储到本地配置文件，程序启动自动加载
- 侧边栏展示书签列表，点击一键应用对应搜索条件
- 支持书签重命名、删除管理

### 6. 全局热键与系统托盘
- 默认全局热键 `Alt + Space`，一键唤起/隐藏主窗口，唤起后自动聚焦搜索框
- 关闭窗口默认最小化到系统托盘，后台常驻不退出
- 托盘图标支持右键菜单：显示主窗口、退出程序
- 支持在设置中自定义热键

### 7. 键盘快捷导航
- 搜索框按 `↓` 直接跳转至结果列表第一项
- 结果列表支持 `↑/↓` 键切换选中项，`Enter` 直接打开文件
- `ESC` 一键清空搜索框内容
- 配套常用快捷键：F5 刷新、Ctrl+D 复制路径、Ctrl+E 打开所在位置

## UI/UX 设计规范
1.  布局结构：顶部搜索栏 + 左侧侧边栏（过滤器+书签切换） + 右侧主结果列表 + 底部状态栏
2.  视觉风格：简洁轻量化，参考 Everything 紧凑布局，降低信息干扰，美观、科学、清晰
    - 列表采用交替行背景色，选中项高亮清晰
    - 不同文件类型展示对应系统默认图标，提升辨识度
    - 字体大小适中，行高紧凑，保证单屏展示更多结果
3.  交互反馈：所有操作给出轻量 SnackBar 提示（如复制成功、删除完成），整体操作自然丝滑
4.  窗口特性：支持调整窗口大小、最大化，自动记忆上次关闭时的窗口尺寸与位置

## 代码架构要求
1.  声明式设计：所有 UI 采用 Flet 控件组合声明，业务逻辑与 UI 层完全分离
2.  模块化拆分：按功能拆分为独立模块，禁止单文件面条代码
    - `search_engine.py`：封装 everything-sdk 调用、异步搜索逻辑、结果处理
    - `file_ops.py`：封装所有文件操作、系统原生调用
    - `bookmarks.py`：书签持久化、增删改查管理
    - `hotkey_tray.py`：全局热键监听、系统托盘管理
    - `ui/` 目录：各界面组件拆分（搜索栏、结果列表、侧边栏、状态栏）
3.  状态管理：统一管理搜索状态、选中状态、过滤器状态，状态变更自动驱动 UI 更新
4.  异常处理：所有 SDK 调用、系统调用增加异常捕获，错误提示友好，程序不崩溃

## 交付要求
- 输出可直接运行的完整项目代码，提供入口文件与 pyproject.toml 依赖说明
- 核心逻辑附带必要注释，关键性能优化点标注说明
- 优先保证搜索流畅度与操作手感，贴近原生 Everything 的使用体验
```

# TODO
- [x] 2026-08-25：去除左侧列，整体采用一列布局，将分类、修改时间、文件大小置于搜索框右侧，搜索框无内容时不显示搜索结果，下方结果处显示书签。合理化整体布局，要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰，操作自然丝滑
- [ ] 2026-08-25：修复BUG，搜索结果的表头：路径与结果的对应内容在左右位置上存在偏差。需要名称与路径左对齐，大小右对齐，修改时间居中对齐。表头存在拖拽区域调整左侧列的宽度
- [ ] 2026-08-25：参考everything，增加搜索结果的运行次数（）
- [ ] 2026-08-25：参考everything，增加搜索结果的运行次数（）